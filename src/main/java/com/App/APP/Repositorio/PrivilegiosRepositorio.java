package com.App.APP.Repositorio;

import org.springframework.data.jpa.repository.JpaRepository;

import com.App.APP.Entity.Privilegios;

public interface PrivilegiosRepositorio extends JpaRepository<Privilegios, Integer> {
    
}
